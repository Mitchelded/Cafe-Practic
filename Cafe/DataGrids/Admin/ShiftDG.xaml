<Page x:Class="Cafe.DataGrids.Admin.ShiftDg"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cafe.DataGrids"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ShiftDG">

    <Grid>
        <StackPanel>
            <DatePicker  Foreground="Black"
         HorizontalAlignment="Center" Width="200" Margin="0,0,0,5" x:Name="dpDate" SelectedDateChanged="dpDate_SelectedDateChanged"/>
            <DataGrid  Margin="0,15,0,20" x:Name="Dg" ScrollViewer.CanContentScroll="True" 
      ScrollViewer.VerticalScrollBarVisibility="Auto"
      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem>

                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
